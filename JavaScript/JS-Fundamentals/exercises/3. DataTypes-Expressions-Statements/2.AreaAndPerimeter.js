function calcRectangleAreaAndPerimeter(width, height){
    let area = width * height;
    let perimeter = (2*width) + (2*height);

    console.log(area + '\n' + perimeter);
}

calcRectangleAreaAndPerimeter(2, 2);